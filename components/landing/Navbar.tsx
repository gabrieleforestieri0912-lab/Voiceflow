'use client';
import type { FC } from 'react';
type Props = { className?: string };
export const Navbar: FC<Props> = () => <nav className='sticky top-0 z-50 border-b border-border bg-background/80 backdrop-blur-md'>Navbar — Fase 1</nav>;
