# Asset pubblici (runtime)

`VITE_PUBLIC` punta qui in dev e a `dist/` in build. Il main legge da questa cartella
l'icona finestra (`favicon.ico`) e l'icona tray (`tray.png`), con fallback inline se assenti.
